'use strict';
var expect = require('chai').expect;

describe("Simple Math tests", function() {
  describe("two equal numbers", function() {
    it("expects 1 to equal 2", function() {
    expect(1).to.eql(1);
    });

    it("converts the basic colors", function() {
      expect(1).to.eql(2);
    });
  });
});
